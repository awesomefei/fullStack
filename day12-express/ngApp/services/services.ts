namespace day12_express.Services {
    export class movieService{
        private movieResource;
        constructor($resource: ng.)
    }
    angular.module('day12_express').service('movieService', movieService);
    }
